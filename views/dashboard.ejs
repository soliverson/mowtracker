<h2>Welcome, <%= user.name %>!</h2>

<div class="dashboard-cards">
  <div class="card">
    <a href="/customers" class="tile">
      <div class="icon">👥</div>
      <div>Customers</div>
    </a>
  </div>
  <div class="card">
    <a href="/calendar" class="tile">
      <div class="icon">📅</div>
      <div>Calendar</div>
    </a>
  </div>
  <div class="card">
    <a href="/payments" class="tile">
      <div class="icon">💰</div>
      <div>Payments</div>
    </a>
  </div>
  <div class="card">
    <a href="/expenses" class="tile">
      <div class="icon">🧾</div>
      <div>Expenses</div>
    </a>
  </div>
  <div class="card">
    <a href="/reports" class="tile">
      <div class="icon">📊</div>
      <div>Reports</div>
    </a>
  </div>
    <div class="card">
    <a href="/fitness" class="tile">
    <div class="icon">🏋️</div>
      <div>Fitness</div>
    </a>
  </div>
  
</div>

<p class="income-display">Total Income: $<%= total.toFixed(2) %></p>

<h3>Upcoming Appointments</h3>
<% if (upcomingJobs.length === 0) { %>
  <p>No upcoming jobs scheduled.</p>
<% } else { %>
  <ul class="upcoming-jobs">
    <% upcomingJobs.forEach(job => { %>
      <li><strong><%= job.customer_name %></strong> - <%= job.date %> <%= job.time || '' %></li>
    <% }) %>
  </ul>
<% } %>
