<h2>Welcome, <%= user.name %>!</h2>

<div class="dashboard-cards">
  <div class="card">
    <a href="/customers">
      <div class="icon">👥</div>
      <h3>Customers</h3>
    </a>
  </div>
  <div class="card">
    <a href="/calendar">
      <div class="icon">📅</div>
      <h3>Calendar</h3>
    </a>
  </div>
  <div class="card">
    <a href="/payments">
      <div class="icon">💰</div>
      <h3>Payments</h3>
    </a>
  </div>
  <div class="card">
    <a href="/reports">
      <div class="icon">📊</div>
      <h3>Reports</h3>
    </a>
  </div>
</div>

<p class="income-display">Total Income: $<%= total.toFixed(2) %></p>

<h3>Upcoming Appointments</h3>
<% if (upcomingJobs.length === 0) { %>
  <p>No upcoming jobs scheduled.</p>
<% } else { %>
  <ul class="upcoming-jobs">
    <% upcomingJobs.forEach(job => { %>
      <li><strong><%= job.customer_name %></strong> - <%= job.date %> <%= job.time || '' %></li>
    <% }) %>
  </ul>
<% } %>
